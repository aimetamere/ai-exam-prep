{
  "name": "ai-class",
  "version": "1.0.0",
  "private": true,
  "description": "Mobile quiz/exam prep frontend",
  "scripts": {
    "generate:questions": "node --experimental-strip-types scripts/generate-question-bank.ts",
    "dev": "vite frontend --host",
    "build": "npm run generate:questions && vite build frontend",
    "preview": "vite preview --host",
    "deploy": "npm run build && npx wrangler deploy",
    "db:migrate": "npx wrangler d1 execute ai-exam-prep-db --file=./migrations/0001_card_status.sql"
  },
  "dependencies": {
    "react": "^19.2.5",
    "react-dom": "^19.2.5"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^5.9.3",
    "vite": "^7.1.12"
  }
}
